---
phase: 01-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-audit/AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "A written classification (ESXi-compatible / needs-rewrite / vCenter-only-remove) exists for each of the 31 MCP tools"
    - "Every tool marked needs-rewrite has its specific vCenter-object references documented with file line numbers and their ESXi equivalents"
    - "The audit document can be referenced directly to drive Phase 2 work without re-reading vmware_manager.py"
  artifacts:
    - path: ".planning/phases/01-audit/AUDIT.md"
      provides: "Complete tool classification table and per-tool rewrite specs"
      contains: "| create_vm | needs-rewrite"
    - path: ".planning/phases/01-audit/AUDIT.md"
      provides: "Rewrite specs for all 8 needs-rewrite tools"
      contains: "datacenter_obj"
  key_links:
    - from: ".planning/phases/01-audit/AUDIT.md classification table"
      to: "vmware_manager.py method bodies"
      via: "line number citations verified against actual file"
      pattern: "line \\d+"
    - from: ".planning/phases/01-audit/AUDIT.md rewrite specs"
      to: ".planning/phases/01-audit/01-RESEARCH.md"
      via: "ESXi equivalent patterns from Architecture Patterns section"
      pattern: "ESXi EQUIVALENT"
---

<objective>
Produce a complete, verified audit document classifying all 31 MCP tools and providing per-tool rewrite specifications for Phase 2 implementors.

Purpose: No production code should change until every tool's fate is decided and documented. This document is the authoritative reference that Phase 2 will execute against.
Output: `.planning/phases/01-audit/AUDIT.md` — a structured Markdown file containing the classification table (AUDIT-01) and rewrite specs (AUDIT-02).
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/01-RESEARCH.md
@esxi_mcp_server/vmware_manager.py
@esxi_mcp_server/mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify tool classifications against live source and produce AUDIT.md classification table</name>
  <files>.planning/phases/01-audit/AUDIT.md</files>
  <action>
Read `esxi_mcp_server/vmware_manager.py` in full. For each of the 31 MCP tools listed in 01-RESEARCH.md, confirm:
1. The method exists at approximately the cited line number (±10 lines is acceptable — cite the verified line)
2. The classification (ESXi-compatible / needs-rewrite / vCenter-only-remove) matches what the method body actually contains

Verification criterion for classification: Does the method body directly reference `self.datacenter_obj`, `datacenter_obj.vmFolder`, `datacenter_obj.datastoreFolder`, `datacenter_obj.networkFolder`, `vim.StoragePod`, or `vim.Datacenter` traversal paths? If yes → needs-rewrite or vCenter-only-remove. If the only datacenter touch is through `self.resource_pool`, `self.datastore_obj`, or `self.network_obj` (objects cached in `_connect_vcenter()` init), do NOT mark as needs-rewrite — those will be fixed by the init rewrite in Phase 3.

Also read `esxi_mcp_server/mcp_server.py` to confirm the tool names as registered in the MCP server match the method names being classified.

Create `.planning/phases/01-audit/AUDIT.md` with:

```
# ESXi MCP Server — Tool Audit

**Date:** 2026-03-02
**Auditor:** Phase 1 plan execution
**Source:** esxi_mcp_server/vmware_manager.py (verified line numbers)

## Classification Summary

- ESXi-compatible: N tools
- needs-rewrite: N tools
- vCenter-only-remove: N tools
- **Total: 31 tools**

## Classification Table

| # | Tool (MCP name) | Method | Classification | vCenter Objects Used | Primary Reason |
|---|-----------------|--------|----------------|---------------------|----------------|
| 1 | `create_vm` | `create_vm()` | needs-rewrite | `datacenter_obj.vmFolder` (line N), `datacenter_obj.datastoreFolder` (line N), `datacenter_obj.networkFolder` (line N) | VM folder and resource lookup via datacenter |
... (all 31 rows)
```

Use the classifications from 01-RESEARCH.md as the starting point. If you find any discrepancy between the research classification and what the live code shows, use the live code and note the discrepancy.

Do NOT mark any tool as needs-rewrite solely because it references `self.resource_pool`, `self.datastore_obj`, or `self.network_obj` — those are init-cached objects fixed by the Phase 3 `_connect_vcenter()` rewrite, not by tool-method rewrites.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
with open('.planning/phases/01-audit/AUDIT.md') as f:
    content = f.read()
# Count table rows (lines starting with | followed by digit)
rows = [l for l in content.splitlines() if re.match(r'\|\s*\d+\s*\|', l)]
assert len(rows) == 31, f'Expected 31 tool rows, found {len(rows)}'
# Check all three classifications appear
for cls in ['ESXi-compatible', 'needs-rewrite', 'vCenter-only-remove']:
    assert cls in content, f'Missing classification: {cls}'
# Check summary section exists
assert 'Classification Summary' in content, 'Missing summary section'
print(f'OK: {len(rows)} tool rows, all 3 classifications present')
"
</automated>
    <manual>Scan the table — confirm `list_datastore_clusters` is vCenter-only-remove, `create_vm`/`clone_vm`/`create_vm_custom` are needs-rewrite, and tools like `list_vms`/`delete_vm` are ESXi-compatible</manual>
  </verify>
  <done>AUDIT.md exists with a 31-row classification table, verified line numbers from live code, and all three classification values present</done>
</task>

<task type="auto">
  <name>Task 2: Append per-tool rewrite specs for all needs-rewrite tools (AUDIT-02)</name>
  <files>.planning/phases/01-audit/AUDIT.md</files>
  <action>
Append a "Rewrite Specifications" section to the AUDIT.md produced in Task 1. For each tool classified as needs-rewrite in Task 1, produce a subsection with:

1. The verified line numbers in `vmware_manager.py` where vCenter-specific objects are referenced
2. The exact Python expression that must change (copy from live code, not from research)
3. The ESXi equivalent expression from 01-RESEARCH.md Architecture Patterns section

Expected needs-rewrite tools (verify against Task 1 output — use Task 1's final list, not this list if they differ):
- `create_vm` — vmFolder, datastoreFolder, networkFolder via datacenter_obj
- `clone_vm` — datacenter_obj.vmFolder fallback
- `create_vm_custom` — same pattern as create_vm
- `upload_file_to_vm` — config.vcenter_host in URL substitution
- `upload_file_to_datastore` — datacenter_obj.name as dcPath param; also config.vcenter_host in upload URL
- `deploy_ovf` — datastoreFolder.childEntity, ContainerView(datacenter_obj), ImportVApp(datacenter_obj.vmFolder), config.vcenter_host in URL
- `deploy_ova` — same pattern as deploy_ovf
- `wait_for_updates` / `_build_traversal_spec` — vim.Datacenter traversal specs in TraversalSpec list

Format for each rewrite spec:

```
### `tool_name`

**Method:** `method_name()` lines N–M

| Location | Current Expression | Change To |
|----------|--------------------|-----------|
| line N | `self.datacenter_obj.vmFolder` | `host_system.vm` (see Pattern 3 in RESEARCH.md) |
| line N | `self.datacenter_obj.datastoreFolder.childEntity` | `CreateContainerView(rootFolder, [vim.Datastore], True)` |
```

Also add a "Phase 2 Implementation Notes" subsection at the end with the open questions from 01-RESEARCH.md:
- `upload_file_to_datastore` dcPath: "ha-datacenter" vs omission — needs live ESXi test
- `wait_for_updates` traversal: simplified rootFolder traversal needs validation on live ESXi
- `upload_file_to_vm` URL wildcard: verify ESXi returns same `*`-host URL format from InitiateFileTransferToGuest

Note on `upload_file_to_vm` and `upload_file_to_datastore` config key references: these also reference `self.config.vcenter_host` which will be renamed to `self.config.esxi_host` in Phase 3. The Phase 2 rewrite should use `self.config.esxi_host` — this assumes Phase 3 renames have already occurred, OR the Phase 2 implementor updates both the config key and the usage site together. Flag this dependency explicitly.
  </action>
  <verify>
    <automated>python3 -c "
with open('.planning/phases/01-audit/AUDIT.md') as f:
    content = f.read()
# Check rewrite specs section exists
assert 'Rewrite Specifications' in content, 'Missing Rewrite Specifications section'
# Check all 8 needs-rewrite tools have spec subsections
tools = ['create_vm', 'clone_vm', 'create_vm_custom', 'upload_file_to_vm',
         'upload_file_to_datastore', 'deploy_ovf', 'deploy_ova', 'wait_for_updates']
for t in tools:
    assert f'### \`{t}\`' in content or f'### {t}' in content, f'Missing rewrite spec for {t}'
# Check phase 2 notes section
assert 'Phase 2 Implementation Notes' in content, 'Missing Phase 2 notes'
print('OK: All 8 rewrite specs present, Phase 2 notes present')
"
</automated>
    <manual>Check one rewrite spec (e.g., deploy_ovf) — confirm it shows current line number, current expression, and ESXi equivalent side-by-side</manual>
  </verify>
  <done>AUDIT.md contains a rewrite spec subsection for each needs-rewrite tool with current expressions, verified line numbers, and ESXi equivalents; plus a Phase 2 implementation notes section flagging open questions</done>
</task>

</tasks>

<verification>
After both tasks complete, run the combined check:

```bash
python3 -c "
import re
with open('.planning/phases/01-audit/AUDIT.md') as f:
    content = f.read()

rows = [l for l in content.splitlines() if re.match(r'\|\s*\d+\s*\|', l)]
assert len(rows) == 31, f'Expected 31 rows, got {len(rows)}'
assert 'vCenter-only-remove' in content
assert 'needs-rewrite' in content
assert 'ESXi-compatible' in content
assert 'Rewrite Specifications' in content
assert 'Phase 2 Implementation Notes' in content

needs_rewrite = [l for l in content.splitlines() if 'needs-rewrite' in l and re.match(r'\|\s*\d+', l)]
print(f'Classification rows: {len(rows)}')
print(f'needs-rewrite tools in table: {len(needs_rewrite)}')
print('AUDIT.md structure: OK')
"
```
</verification>

<success_criteria>
1. `.planning/phases/01-audit/AUDIT.md` exists
2. Classification table has exactly 31 rows, one per tool, with verified line numbers from live vmware_manager.py
3. All 8 needs-rewrite tools have a rewrite spec with current expression, line number, and ESXi equivalent
4. `list_datastore_clusters` is classified vCenter-only-remove
5. Phase 2 implementors can execute against AUDIT.md without opening vmware_manager.py to re-classify
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-01-SUMMARY.md` using the summary template at `@/home/cars/.claude/get-shit-done/templates/summary.md`.
</output>
