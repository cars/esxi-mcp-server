---
phase: 04-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-04

must_haves:
  truths:
    - "CLAUDE.md Project Overview says '30 MCP tools' — not '31'"
    - "CLAUDE.md Project Overview describes ESXi hosts — not ESXi/vCenter infrastructure"
    - "CLAUDE.md Configuration section lists ESXI_HOST, ESXI_USER, ESXI_PASSWORD as required — no VCENTER_* names"
    - "CLAUDE.md Configuration section has no VCENTER_DATACENTER or VCENTER_CLUSTER entries"
    - "CLAUDE.md __main__.py description says 'connects to ESXi' — not 'connects to vCenter'"
    - "CLAUDE.md mcp_server.py description says '30 tool definitions' — not '31'"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Context file for future Claude Code sessions in this repository"
      contains: "30 MCP tools"
  key_links:
    - from: "CLAUDE.md tool count"
      to: "esxi_mcp_server/mcp_server.py types.Tool( registrations"
      via: "tool count must match actual registered tools (30 after list_datastore_clusters removal)"
      pattern: "30"
    - from: "CLAUDE.md Configuration env var list"
      to: "esxi_mcp_server/config.py env_map"
      via: "env vars listed must match env_map keys"
      pattern: "ESXI_HOST|ESXI_USER|ESXI_PASSWORD"
---

<objective>
Update CLAUDE.md so future Claude Code sessions start with accurate context: correct tool count (30), ESXi-only description, correct config key names (ESXI_*), and correct __main__.py description.

Purpose: CLAUDE.md is read by Claude Code at session start to understand the project. It currently says "31 MCP tools", "ESXi/vCenter infrastructure", "VCENTER_HOST", "VCENTER_DATACENTER", "VCENTER_CLUSTER", and "connects to vCenter". All four are wrong after Phases 2 and 3. Future Claude sessions will have wrong context causing incorrect assumptions.
Output: CLAUDE.md reflecting the current state: 30 tools, ESXi-only, ESXI_* config keys.
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-RESEARCH.md

<interfaces>
<!-- Current CLAUDE.md outdated sections (verified during research) -->
<!-- Make targeted edits to these specific locations only -->

Line 7 (Project Overview paragraph):
  Current: "...server for managing VMware ESXi/vCenter infrastructure. It exposes 31 MCP tools..."
  Must become: "...server for managing VMware ESXi hosts. It exposes 30 MCP tools..."

Line 61 (mcp_server.py description):
  Current: "...registers all 31 tool definitions..."
  Must become: "...registers all 30 tool definitions..."
  Also note: list_datastore_clusters was removed; mention removed tool name for clarity.

Line 66 (__main__.py description):
  Current: "...connects to vCenter, creates MCP server..."
  Must become: "...connects to ESXi, creates MCP server..."

Lines 78-80 (Configuration section):
  Current:
    - Required: `VCENTER_HOST`, `VCENTER_USER`, `VCENTER_PASSWORD`
    - Optional: `VCENTER_DATACENTER`, `VCENTER_CLUSTER`, `VCENTER_DATASTORE`, `VCENTER_NETWORK`, `VCENTER_INSECURE`
    - Server: `MCP_API_KEY`, `MCP_LOG_FILE`, `MCP_LOG_LEVEL`
  Must become:
    - Required: `ESXI_HOST`, `ESXI_USER`, `ESXI_PASSWORD`
    - Optional: `ESXI_DATASTORE`, `ESXI_NETWORK`, `ESXI_INSECURE`
    - Server: `MCP_API_KEY`, `MCP_LOG_FILE`, `MCP_LOG_LEVEL`

Authoritative values:
  Tool count: 30 (verified: grep -c 'types\.Tool(' mcp_server.py = 30)
  Removed tool: list_datastore_clusters
  Required env vars: ESXI_HOST, ESXI_USER, ESXI_PASSWORD
  Optional env vars: ESXI_DATASTORE, ESXI_NETWORK, ESXI_INSECURE
  Server env vars: MCP_API_KEY, MCP_LOG_FILE, MCP_LOG_LEVEL
  REMOVED: VCENTER_DATACENTER, VCENTER_CLUSTER
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md tool count, description, and configuration section</name>
  <files>CLAUDE.md</files>
  <action>
Make the following targeted edits to CLAUDE.md. Read the file first to confirm exact current text before editing.

Edit 1 — Project Overview paragraph (line 7):
  Find: "server for managing VMware ESXi/vCenter infrastructure. It exposes 31 MCP tools"
  Replace with: "server for managing VMware ESXi hosts. It exposes 30 MCP tools"

Edit 2 — mcp_server.py key module description (line ~61):
  Find: "registers all 31 tool definitions with JSON schemas plus resource handlers"
  Replace with: "registers all 30 tool definitions with JSON schemas plus resource handlers. (`list_datastore_clusters` was removed — vCenter-only StoragePod concept)"

Edit 3 — __main__.py key module description (line ~66):
  Find: "connects to vCenter, creates MCP server"
  Replace with: "connects to ESXi, creates MCP server"

Edit 4 — Configuration section env var list (lines ~78-80):
  Find:
    - Required: `VCENTER_HOST`, `VCENTER_USER`, `VCENTER_PASSWORD`
    - Optional: `VCENTER_DATACENTER`, `VCENTER_CLUSTER`, `VCENTER_DATASTORE`, `VCENTER_NETWORK`, `VCENTER_INSECURE`
    - Server: `MCP_API_KEY`, `MCP_LOG_FILE`, `MCP_LOG_LEVEL`
  Replace with:
    - Required: `ESXI_HOST`, `ESXI_USER`, `ESXI_PASSWORD`
    - Optional: `ESXI_DATASTORE`, `ESXI_NETWORK`, `ESXI_INSECURE`
    - Server: `MCP_API_KEY`, `MCP_LOG_FILE`, `MCP_LOG_LEVEL`

These are targeted edits. Do NOT rewrite sections that are already correct (the Commands, Architecture diagram, Module Descriptions other than mcp_server.py and __main__.py, and Docker sections are all accurate and must not change).
  </action>
  <verify>
    <automated>grep -n "VCENTER_HOST\|VCENTER_DATACENTER\|VCENTER_CLUSTER\|31 MCP\|ESXi/vCenter\|vCenter infrastructure\|connects to vCenter" /home/cars/src/github/cars/esxi-mcp-server/CLAUDE.md</automated>
  </verify>
  <done>grep returns zero matches — no VCENTER_HOST, VCENTER_DATACENTER, VCENTER_CLUSTER, "31 MCP", "ESXi/vCenter", "vCenter infrastructure", or "connects to vCenter" references remain in CLAUDE.md; "30 MCP tools", "ESXI_HOST", "ESXI_USER", "ESXI_PASSWORD" are present</done>
</task>

</tasks>

<verification>
After task completes:

```bash
# Verify no outdated vCenter references in key sections
grep -n "VCENTER_HOST\|VCENTER_DATACENTER\|VCENTER_CLUSTER\|31 MCP\|ESXi/vCenter infrastructure\|connects to vCenter" /home/cars/src/github/cars/esxi-mcp-server/CLAUDE.md

# Verify new content present
grep -n "30 MCP\|ESXI_HOST\|ESXI_USER\|ESXI_PASSWORD\|connects to ESXi" /home/cars/src/github/cars/esxi-mcp-server/CLAUDE.md

# Verify removed tool noted
grep -n "list_datastore_clusters" /home/cars/src/github/cars/esxi-mcp-server/CLAUDE.md
```

First grep: zero matches. Second grep: multiple matches. Third grep: at least 1 match.
</verification>

<success_criteria>
1. CLAUDE.md Project Overview says "30 MCP tools" — not "31"
2. CLAUDE.md Project Overview describes "managing VMware ESXi hosts" — not "ESXi/vCenter infrastructure"
3. CLAUDE.md mcp_server.py description says "30 tool definitions" and notes list_datastore_clusters was removed
4. CLAUDE.md __main__.py description says "connects to ESXi" — not "connects to vCenter"
5. CLAUDE.md Configuration section lists ESXI_HOST, ESXI_USER, ESXI_PASSWORD as required
6. CLAUDE.md Configuration section has no VCENTER_DATACENTER or VCENTER_CLUSTER entries
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-04-SUMMARY.md` with:
- What changed in CLAUDE.md (which lines, what replaced)
- Verification commands run and their output
- Confirmation that future Claude sessions will start with accurate context
</output>
