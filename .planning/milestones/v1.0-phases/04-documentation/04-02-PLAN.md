---
phase: 04-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml.sample
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "config.yaml.sample uses esxi_host, esxi_user, esxi_password as YAML keys"
    - "config.yaml.sample has no datacenter or cluster keys"
    - "config.yaml.sample has no vcenter_* keys"
    - "All YAML keys match Config dataclass field names exactly (lowercase, underscore)"
    - "No YAML key is in ALL_CAPS (ALL_CAPS keys cause TypeError on startup)"
  artifacts:
    - path: "config.yaml.sample"
      provides: "Reference configuration template for ESXi-only server"
      contains: "esxi_host"
  key_links:
    - from: "config.yaml.sample YAML keys"
      to: "esxi_mcp_server/config.py Config dataclass fields"
      via: "YAML loader passes keys as kwargs to Config(**config_data)"
      pattern: "esxi_host|esxi_user|esxi_password"
---

<objective>
Replace config.yaml.sample with a corrected version that uses YAML keys matching the current Config dataclass field names (esxi_host, esxi_user, esxi_password) and omits the removed datacenter and cluster fields entirely.

Purpose: config.yaml.sample is the authoritative template users copy to create their config.yaml. It currently has vcenter_* keys and datacenter/cluster fields that no longer exist in the Config dataclass — using this file as-is causes a TypeError on startup.
Output: config.yaml.sample with correct ESXi field names, ESXi-appropriate comments, and no removed fields.
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-RESEARCH.md

<interfaces>
<!-- Config dataclass field names — these are the exact YAML keys the loader accepts -->
<!-- Source: esxi_mcp_server/config.py -->

Valid YAML keys (match Config dataclass fields):
  esxi_host     — required, ESXi host IP or hostname
  esxi_user     — required, ESXi login username
  esxi_password — required, ESXi login password
  datastore     — optional, default datastore name
  network       — optional, default network name
  insecure      — optional, skip SSL cert verification (bool)
  api_key       — optional, API key for MCP authentication
  log_file      — optional, log file path
  log_level     — optional, DEBUG/INFO/WARNING/ERROR

INVALID keys (removed from Config dataclass — cause TypeError if provided):
  datacenter    — REMOVED in Phase 3
  cluster       — REMOVED in Phase 3
  vcenter_host  — RENAMED to esxi_host in Phase 3
  vcenter_user  — RENAMED to esxi_user in Phase 3
  vcenter_password — RENAMED to esxi_password in Phase 3

CRITICAL: YAML keys must be lowercase with underscores (esxi_host), NOT all-caps (ESXI_HOST).
All-caps keys are env var names, not YAML field names. The YAML loader uses field names.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite config.yaml.sample with correct ESXi field names</name>
  <files>config.yaml.sample</files>
  <action>
Replace the entire contents of config.yaml.sample with:

```yaml
esxi_host: "192.168.0.1"           # ESXi host IP address or hostname
esxi_user: "root"                   # ESXi username
esxi_password: "s3cr3t"            # ESXi password
datastore: "datastore1"            # Default datastore name (optional)
network: "VM Network"              # Default network name (optional)
insecure: true                     # Skip SSL certificate verification
api_key: "s3cr3t-api-key"         # API key for MCP authentication (optional)
log_file: "./logs/esxi_mcp.log"   # Log file path (optional)
log_level: "DEBUG"                 # Log level: DEBUG/INFO/WARNING/ERROR
```

Do NOT include datacenter or cluster keys — those fields have been removed from the Config dataclass. Including them causes a TypeError: unexpected keyword argument on startup.

Do NOT use ALL_CAPS key names (ESXI_HOST etc.) — those are environment variable names, not YAML keys. YAML keys must match the Config dataclass field names exactly.
  </action>
  <verify>
    <automated>cat /home/cars/src/github/cars/esxi-mcp-server/config.yaml.sample</automated>
  </verify>
  <done>config.yaml.sample contains esxi_host, esxi_user, esxi_password as the first three keys; no vcenter_* keys present; no datacenter or cluster keys present; no ALL_CAPS keys present</done>
</task>

</tasks>

<verification>
After task completes:

```bash
# Verify no old keys remain
grep -n "vcenter\|datacenter\|cluster\|VCENTER" /home/cars/src/github/cars/esxi-mcp-server/config.yaml.sample

# Verify new keys are present
grep -n "esxi_host\|esxi_user\|esxi_password" /home/cars/src/github/cars/esxi-mcp-server/config.yaml.sample

# Verify no ALL_CAPS keys (which would be wrong YAML field names)
grep -n "^[A-Z]" /home/cars/src/github/cars/esxi-mcp-server/config.yaml.sample
```

First grep should return zero matches. Second grep should return 3 matches. Third grep should return zero matches.
</verification>

<success_criteria>
1. config.yaml.sample has esxi_host, esxi_user, esxi_password as the required fields
2. No vcenter_host, vcenter_user, vcenter_password keys present
3. No datacenter or cluster keys present
4. No ALL_CAPS YAML keys (would be env var names, not field names)
5. Optional fields (datastore, network, insecure, api_key, log_file, log_level) all present with appropriate comments
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-02-SUMMARY.md` with:
- What changed in config.yaml.sample
- Verification commands run and their output
- Confirmation that all keys match Config dataclass field names
</output>
