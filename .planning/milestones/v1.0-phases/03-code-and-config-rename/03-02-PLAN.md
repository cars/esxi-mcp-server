---
phase: 03-code-and-config-rename
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - esxi_mcp_server/vmware_manager.py
  - esxi_mcp_server/__init__.py
autonomous: true
requirements: [CODE-01, CONF-04, CODE-03, CODE-04]

must_haves:
  truths:
    - "_connect_esxi() method exists in VMwareManager; _connect_vcenter() does not"
    - "All call sites (_ensure_connected and __init__) call _connect_esxi(), not _connect_vcenter()"
    - "Datacenter lookup in _connect_esxi() is unconditional (no if self.config.datacenter branch)"
    - "Cluster lookup in _connect_esxi() is unconditional (no if self.config.cluster branch)"
    - "No vcenter_user or vcenter_password field references remain in vmware_manager.py"
    - "No 'vCenter' in log messages or docstrings (except the factual clone_vm comment which is rephrased)"
    - "__init__.py module docstring references ESXi only, not vCenter"
  artifacts:
    - path: "esxi_mcp_server/vmware_manager.py"
      provides: "Renamed _connect_esxi method, simplified connection logic, updated field refs and comments"
      contains: "_connect_esxi"
    - path: "esxi_mcp_server/__init__.py"
      provides: "Updated module docstring"
      contains: "ESXi management server"
  key_links:
    - from: "VMwareManager.__init__"
      to: "_connect_esxi()"
      via: "self._connect_esxi() call at line 25"
      pattern: "self._connect_esxi\\(\\)"
    - from: "_ensure_connected"
      to: "_connect_esxi()"
      via: "self._connect_esxi() call (was line 36)"
      pattern: "self._connect_esxi\\(\\)"
    - from: "clone_vm source_url"
      to: "self.config.esxi_user / self.config.esxi_password"
      via: "f-string references in vi:// URL construction"
      pattern: "self\\.config\\.esxi_user"
---

<objective>
Rename _connect_vcenter() to _connect_esxi(), remove datacenter/cluster conditional branches from the connection method (leaving only the unconditional ESXi path), update all self.config.vcenter_user/vcenter_password field references, update all comments and docstrings referencing vCenter, and fix the __init__.py module docstring.

Purpose: This plan completes the Phase 3 rename by updating vmware_manager.py — the largest file and the one containing the bulk of the vcenter surface. It depends on Plan 01 (config.py) having already renamed the Config dataclass fields so self.config.esxi_user and self.config.esxi_password are valid.
Output: vmware_manager.py with _connect_esxi(), simplified unconditional connection logic, no vcenter field refs, and updated comments. __init__.py with corrected module docstring.
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-and-config-rename/03-RESEARCH.md
@.planning/phases/03-code-and-config-rename/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename _connect_vcenter to _connect_esxi and remove datacenter/cluster conditional branches</name>
  <files>esxi_mcp_server/vmware_manager.py</files>
  <action>
    Edit esxi_mcp_server/vmware_manager.py to apply all changes in a single pass. Read the full file first.

    **A. Method rename — three occurrences (update ALL):**
    - Line 25: `self._connect_vcenter()` → `self._connect_esxi()`
    - Line 36: `self._connect_vcenter()` → `self._connect_esxi()`
    - Line 38: `def _connect_vcenter(self):` → `def _connect_esxi(self):`

    **B. Docstring and log message updates:**
    - Line 28 docstring: "Check if the vCenter session is still active and reconnect if needed." → "Check if the ESXi session is still active and reconnect if needed."
    - Line 35 log: `"vCenter/ESXi session expired or lost, reconnecting..."` → `"ESXi session expired or lost, reconnecting..."`
    - Line 39 docstring: "Connect to vCenter/ESXi and retrieve main resource object references." → "Connect to ESXi and retrieve main resource object references."
    - Line 58 log: `"Failed to connect to vCenter/ESXi: {e}"` → `"Failed to connect to ESXi: {e}"`
    - Line 62 log: `"Successfully connected to VMware vCenter/ESXi API"` → `"Successfully connected to VMware ESXi API"`
    - Line 536 docstring: "Clone a VM using ovftool (ESXi-compatible; CloneVM_Task requires vCenter)." → "Clone a VM using ovftool (CloneVM_Task is not supported on standalone ESXi hosts)."
    - Line 722 docstring: "Wait for a vCenter task to complete or timeout." → "Wait for a task to complete or timeout."

    **C. Field reference updates — four occurrences in _connect_esxi():**
    - Line 48: `user=self.config.vcenter_user,` → `user=self.config.esxi_user,`
    - Line 49: `pwd=self.config.vcenter_password,` → `pwd=self.config.esxi_password,`
    - Line 55: `user=self.config.vcenter_user,` → `user=self.config.esxi_user,`
    - Line 56: `pwd=self.config.vcenter_password)` → `pwd=self.config.esxi_password)`

    **D. Field reference updates — two occurrences in clone_vm() vi:// URLs:**
    - Line 548: `self.config.vcenter_user` → `self.config.esxi_user`, `self.config.vcenter_password` → `self.config.esxi_password`
    - Line 552: same replacements

    **E. Dead code removal — datacenter lookup simplification (lines 64-77):**
    Replace the entire if/else block that branches on `self.config.datacenter` with the unconditional ESXi path only:
    ```python
    # Retrieve target datacenter object
    self.datacenter_obj = next((dc for dc in self.content.rootFolder.childEntity
                                if isinstance(dc, vim.Datacenter)), None)
    if not self.datacenter_obj:
        raise Exception("No datacenter found on ESXi host")
    ```
    Remove the lines that reference `self.config.datacenter` — they reference a field that no longer exists in the Config dataclass.

    **F. Dead code removal — cluster lookup simplification (lines 79-98):**
    Replace the entire if/else block that branches on `self.config.cluster` with the unconditional ESXi path only:
    ```python
    # Retrieve resource pool (standalone ESXi always has exactly one ComputeResource)
    compute_resource = next((cr for cr in self.datacenter_obj.hostFolder.childEntity
                              if isinstance(cr, vim.ComputeResource)), None)
    if not compute_resource:
        raise Exception("No compute resource found on ESXi host")
    self.resource_pool = compute_resource.resourcePool
    self.compute_resource = compute_resource
    logging.info(f"Using resource pool: {self.resource_pool.name}")
    ```
    Remove all lines referencing `self.config.cluster` or `vim.ClusterComputeResource` — the cluster field no longer exists.

    After all edits, run a mental diff to confirm: no `vcenter_user`, `vcenter_password`, `_connect_vcenter`, `self.config.datacenter`, or `self.config.cluster` references remain. The string "vCenter" at line 536 must also be gone (rephrased per step B).
  </action>
  <verify>
    <automated>cd /home/cars/src/github/cars/esxi-mcp-server && python -c "
import ast, sys
with open('esxi_mcp_server/vmware_manager.py') as f:
    src = f.read()
# Check method renamed
assert '_connect_esxi' in src, 'FAIL: _connect_esxi not found'
assert '_connect_vcenter' not in src, 'FAIL: _connect_vcenter still present'
# Check field refs gone
assert 'vcenter_user' not in src, 'FAIL: vcenter_user still in vmware_manager.py'
assert 'vcenter_password' not in src, 'FAIL: vcenter_password still in vmware_manager.py'
# Check dead config field refs gone
assert 'self.config.datacenter' not in src, 'FAIL: self.config.datacenter still present'
assert 'self.config.cluster' not in src, 'FAIL: self.config.cluster still present'
# Verify file parses
ast.parse(src)
print('PASS: vmware_manager.py has _connect_esxi, no vcenter field refs, parses OK')
"
    </automated>
    <manual>Run: grep -n "vcenter\|vCenter\|VCENTER" esxi_mcp_server/vmware_manager.py — only acceptable hit is none (the clone_vm docstring was rephrased).</manual>
  </verify>
  <done>vmware_manager.py has def _connect_esxi() at the definition site; all three call/definition occurrences updated; self.config.esxi_user and self.config.esxi_password used throughout; no if self.config.datacenter or if self.config.cluster branches remain; all log messages and docstrings reference ESXi only; file parses without error.</done>
</task>

<task type="auto">
  <name>Task 2: Update __init__.py module docstring and run full vcenter grep verification</name>
  <files>esxi_mcp_server/__init__.py</files>
  <action>
    Edit esxi_mcp_server/__init__.py line 1:
    Before: `"""ESXi MCP Server - A VMware ESXi/vCenter management server based on MCP."""`
    After:  `"""ESXi MCP Server - A VMware ESXi management server based on MCP."""`

    Then run the authoritative post-change verification grep across all Python files:
    ```bash
    grep -rn "vcenter\|vCenter\|VCENTER" esxi_mcp_server/ --include="*.py"
    ```
    The output must be zero lines (completely empty). If any hit appears, fix it before marking this task done.
  </action>
  <verify>
    <automated>cd /home/cars/src/github/cars/esxi-mcp-server && python -c "
import subprocess, sys
result = subprocess.run(
    ['grep', '-rn', 'vcenter|vCenter|VCENTER', 'esxi_mcp_server/', '--include=*.py'],
    capture_output=True, text=True
)
hits = [l for l in result.stdout.splitlines() if l.strip()]
if hits:
    print('FAIL: vcenter references still present:')
    for h in hits:
        print(h)
    sys.exit(1)
print('PASS: zero vcenter/vCenter/VCENTER references remain in esxi_mcp_server/*.py')
" 2>/dev/null || grep -rn "vcenter\|vCenter\|VCENTER" /home/cars/src/github/cars/esxi-mcp-server/esxi_mcp_server/ --include="*.py"; echo "Exit: $?"
    </automated>
    <manual>Visually confirm __init__.py line 1 reads: """ESXi MCP Server - A VMware ESXi management server based on MCP."""</manual>
  </verify>
  <done>__init__.py module docstring says "ESXi management server" without "vCenter"; grep across all esxi_mcp_server/*.py files returns zero lines matching vcenter|vCenter|VCENTER.</done>
</task>

</tasks>

<verification>
Final phase 3 verification — all success criteria must be TRUE:
1. `grep -rn "vcenter\|vCenter\|VCENTER" esxi_mcp_server/ --include="*.py"` returns zero lines
2. `python -c "import ast; ast.parse(open('esxi_mcp_server/vmware_manager.py').read()); print('OK')"` succeeds
3. `python -c "import ast; ast.parse(open('esxi_mcp_server/config.py').read()); print('OK')"` succeeds
4. `python -c "from esxi_mcp_server.config import load_config; import os; os.environ.update({'ESXI_HOST':'h','ESXI_USER':'u','ESXI_PASSWORD':'p'}); c=load_config(); assert c.esxi_user=='u'; print('OK')"` succeeds
5. `grep "_connect_vcenter" esxi_mcp_server/vmware_manager.py` returns zero lines
6. `grep "_connect_esxi" esxi_mcp_server/vmware_manager.py` returns 3 lines (definition + 2 call sites)
</verification>

<success_criteria>
- _connect_esxi() defined and called at both __init__ and _ensure_connected; _connect_vcenter() does not exist
- Datacenter and cluster lookup in _connect_esxi() are unconditional single-path (no if/else branch on config.datacenter or config.cluster)
- No self.config.vcenter_user or self.config.vcenter_password references anywhere in vmware_manager.py
- grep -rn "vcenter|vCenter|VCENTER" esxi_mcp_server/ --include="*.py" returns zero lines
- All Python files parse without AST errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-and-config-rename/03-02-SUMMARY.md`
</output>
