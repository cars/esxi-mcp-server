---
phase: 02-tool-changes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - esxi_mcp_server/vmware_manager.py
autonomous: true
requirements:
  - RWRT-05
gap_closure: true

must_haves:
  truths:
    - "_connect_vcenter() reads self.config.esxi_host at both SSL and standard connection paths"
    - "Server reaches the SmartConnect call without AttributeError on startup"
  artifacts:
    - path: "esxi_mcp_server/vmware_manager.py"
      provides: "_connect_vcenter with esxi_host references at lines 47 and 54"
      contains: "self.config.esxi_host"
  key_links:
    - from: "esxi_mcp_server/config.py"
      to: "esxi_mcp_server/vmware_manager.py _connect_vcenter"
      via: "self.config.esxi_host attribute access"
      pattern: "self\\.config\\.esxi_host"
---

<objective>
Fix the AttributeError that prevents server startup: update `_connect_vcenter()` in `vmware_manager.py` to reference `self.config.esxi_host` instead of the renamed-away `self.config.vcenter_host`.

Purpose: Plan 02-04 renamed `Config.vcenter_host` to `Config.esxi_host` and updated the 4 URL substitution call sites, but explicitly deferred updating `_connect_vcenter()` to Phase 3. That deferral created a broken state — the Config dataclass no longer has a `vcenter_host` attribute, so `_connect_vcenter()` raises `AttributeError: 'Config' object has no attribute 'vcenter_host'` on every connection attempt before any MCP tool can execute.

Output: `vmware_manager.py` with `self.config.esxi_host` at lines 47 and 54 — two-line fix, no other changes.
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vcenter_host references in _connect_vcenter</name>
  <files>esxi_mcp_server/vmware_manager.py</files>
  <action>
    In `_connect_vcenter()` (lines 40-62 of vmware_manager.py), two SmartConnect calls reference `self.config.vcenter_host` which no longer exists on the Config dataclass (was renamed to `esxi_host` in plan 02-04).

    Make exactly two changes, nothing else:

    Line 47 — SSL path:
      BEFORE: `host=self.config.vcenter_host,`
      AFTER:  `host=self.config.esxi_host,`

    Line 54 — standard path:
      BEFORE: `host=self.config.vcenter_host,`
      AFTER:  `host=self.config.esxi_host,`

    Do NOT change any other lines. Do NOT rename `_connect_vcenter` (that is Phase 3 scope). Do NOT touch `vcenter_user` or `vcenter_password` references on lines 48-49 and 55-56 (those renames are also deferred to Phase 3 per the decisions log in STATE.md).
  </action>
  <verify>
    <automated>grep -n "self\.config\.vcenter_host" /home/cars/src/github/cars/esxi-mcp-server/esxi_mcp_server/vmware_manager.py | wc -l</automated>
    <manual>Output must be 0. Also confirm: grep -n "self\.config\.esxi_host" vmware_manager.py should show at least 6 hits (lines 47, 54 newly added, plus the 4 URL substitution sites from plan 02-04).</manual>
  </verify>
  <done>Zero occurrences of `self.config.vcenter_host` remain in vmware_manager.py. Lines 47 and 54 read `host=self.config.esxi_host,`. All other lines unchanged.</done>
</task>

</tasks>

<verification>
After the fix, verify the full attribute removal is complete:

1. No `vcenter_host` attribute reference anywhere in vmware_manager.py:
   `grep -n "vcenter_host" /home/cars/src/github/cars/esxi-mcp-server/esxi_mcp_server/vmware_manager.py`
   Expected: zero results

2. `esxi_host` appears at all 6 expected sites (2 new + 4 from plan 02-04):
   `grep -n "esxi_host" /home/cars/src/github/cars/esxi-mcp-server/esxi_mcp_server/vmware_manager.py`
   Expected: lines 47, 54, 938, 978, 1076, 1228 (line numbers approximate after edits)

3. Config dataclass has no `vcenter_host` field:
   `grep -n "vcenter_host" /home/cars/src/github/cars/esxi-mcp-server/esxi_mcp_server/config.py`
   Expected: only the env-var mapping `"VCENTER_HOST": "esxi_host"` (backward-compat alias), no field definition
</verification>

<success_criteria>
- `self.config.vcenter_host` does not appear anywhere in vmware_manager.py
- `_connect_vcenter()` lines 47 and 54 both reference `self.config.esxi_host`
- `config.py` `Config` dataclass `esxi_host` field is unchanged
- No other lines in vmware_manager.py were modified
- RWRT-05 gap is closed: server can reach SmartConnect without AttributeError
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-changes/02-tool-changes-05-SUMMARY.md` with:
- What was changed (2 lines in _connect_vcenter)
- Why it was deferred and why it must be fixed now
- Verification results confirming zero remaining vcenter_host references
</output>
