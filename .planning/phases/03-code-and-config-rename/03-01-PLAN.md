---
phase: 03-code-and-config-rename
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - esxi_mcp_server/config.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CODE-02]

must_haves:
  truths:
    - "Server starts when ESXI_USER and ESXI_PASSWORD environment variables are set"
    - "Server rejects startup when only VCENTER_USER is set (not read)"
    - "VCENTER_DATACENTER and VCENTER_CLUSTER env vars are not present in env_map"
    - "Config dataclass has esxi_user and esxi_password fields, not vcenter_user/vcenter_password"
    - "Config dataclass has no datacenter or cluster fields"
  artifacts:
    - path: "esxi_mcp_server/config.py"
      provides: "Renamed Config dataclass and env_map"
      contains: "esxi_user"
  key_links:
    - from: "env_map ESXI_USER"
      to: "Config.esxi_user"
      via: "env_map value must exactly match dataclass field name"
      pattern: "\"ESXI_USER\": \"esxi_user\""
    - from: "required_keys"
      to: "Config dataclass fields"
      via: "required_keys list values must match renamed field names"
      pattern: "required_keys = \\[\"esxi_host\", \"esxi_user\", \"esxi_password\"\\]"
---

<objective>
Rename all config.py env var keys and Config dataclass fields from vCenter to ESXi terminology, and remove the datacenter/cluster fields that no longer exist.

Purpose: config.py is the root dependency for Phase 3 — vmware_manager.py field references like self.config.vcenter_user must be updated to match the renamed dataclass fields. Doing config.py first in Wave 1 ensures Wave 2 (vmware_manager.py) can safely reference correct field names.
Output: Updated config.py with ESXI_* env keys, esxi_user/esxi_password dataclass fields, no datacenter/cluster fields, and a synchronized required_keys list.
</objective>

<execution_context>
@/home/cars/.claude/get-shit-done/workflows/execute-plan.md
@/home/cars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-and-config-rename/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Config dataclass fields and env_map in config.py</name>
  <files>esxi_mcp_server/config.py</files>
  <action>
    Edit esxi_mcp_server/config.py to apply all of the following changes in one pass:

    **Dataclass field renames (lines 13-16):**
    - Rename `vcenter_user: str` → `esxi_user: str`
    - Rename `vcenter_password: str` → `esxi_password: str`
    - Remove `datacenter: Optional[str] = None` line entirely
    - Remove `cluster: Optional[str] = None` line entirely

    **env_map replacement (lines 54-66):** Replace the entire env_map dict with:
    ```python
    env_map = {
        "ESXI_HOST": "esxi_host",
        "ESXI_USER": "esxi_user",
        "ESXI_PASSWORD": "esxi_password",
        "ESXI_DATASTORE": "datastore",
        "ESXI_NETWORK": "network",
        "ESXI_INSECURE": "insecure",
        "MCP_API_KEY": "api_key",
        "MCP_LOG_FILE": "log_file",
        "MCP_LOG_LEVEL": "log_level"
    }
    ```
    This removes VCENTER_HOST, VCENTER_USER, VCENTER_PASSWORD, VCENTER_DATACENTER, VCENTER_CLUSTER, VCENTER_DATASTORE, VCENTER_NETWORK, VCENTER_INSECURE entirely.

    **required_keys update (line 78):** Change:
    ```python
    required_keys = ["esxi_host", "vcenter_user", "vcenter_password"]
    ```
    to:
    ```python
    required_keys = ["esxi_host", "esxi_user", "esxi_password"]
    ```

    Do NOT rename `datastore` or `network` dataclass fields — only the env var keys change for those (CONF-03 renames env keys, not field names). The `config.datastore` and `config.network` field names remain unchanged throughout the codebase.
  </action>
  <verify>
    <automated>cd /home/cars/src/github/cars/esxi-mcp-server && python -c "
from esxi_mcp_server.config import Config, load_config
import os
os.environ['ESXI_HOST'] = 'test-host'
os.environ['ESXI_USER'] = 'testuser'
os.environ['ESXI_PASSWORD'] = 'testpass'
cfg = load_config()
assert cfg.esxi_host == 'test-host', f'esxi_host wrong: {cfg.esxi_host}'
assert cfg.esxi_user == 'testuser', f'esxi_user wrong: {cfg.esxi_user}'
assert cfg.esxi_password == 'testpass', f'esxi_password wrong: {cfg.esxi_password}'
assert not hasattr(cfg, 'datacenter'), 'datacenter field still exists'
assert not hasattr(cfg, 'cluster'), 'cluster field still exists'
print('PASS: Config loads with ESXI_* env vars and has correct fields')
"
    </automated>
    <manual>Confirm grep returns zero hits for old keys: grep -n "VCENTER_USER\|VCENTER_PASSWORD\|VCENTER_DATACENTER\|VCENTER_CLUSTER\|vcenter_user\|vcenter_password\|datacenter\|cluster" esxi_mcp_server/config.py</manual>
  </verify>
  <done>Config dataclass has esxi_user and esxi_password fields (no vcenter_user/vcenter_password, no datacenter/cluster); env_map uses only ESXI_* and MCP_* keys; required_keys lists esxi_user and esxi_password; load_config() succeeds with ESXI_HOST/ESXI_USER/ESXI_PASSWORD env vars.</done>
</task>

</tasks>

<verification>
After task completion:
1. `grep -n "VCENTER\|vcenter_user\|vcenter_password\|datacenter\|cluster" esxi_mcp_server/config.py` returns zero lines (no old keys remain)
2. `python -c "from esxi_mcp_server.config import Config; import inspect; print(inspect.signature(Config))"` shows `esxi_user` and `esxi_password` parameters, no `datacenter` or `cluster`
3. `python -c "from esxi_mcp_server.config import load_config; import os; os.environ.update({'ESXI_HOST':'h','ESXI_USER':'u','ESXI_PASSWORD':'p'}); load_config()"` executes without error
</verification>

<success_criteria>
- Config.esxi_user and Config.esxi_password fields exist; Config.vcenter_user, Config.vcenter_password, Config.datacenter, Config.cluster do not exist
- env_map contains ESXI_HOST, ESXI_USER, ESXI_PASSWORD, ESXI_DATASTORE, ESXI_NETWORK, ESXI_INSECURE as its only ESXI_/VCENTER_ keys
- required_keys = ["esxi_host", "esxi_user", "esxi_password"]
- load_config() works correctly with ESXI_* environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-and-config-rename/03-01-SUMMARY.md`
</output>
